import {StyleSheet } from 'react-native';

export const global_styles = StyleSheet.create({
    button: {
        backgroundColor: '#125625', // Yellow button color
        paddingVertical: 18,
        paddingHorizontal: 24,
        borderRadius: 10,
        marginTop: 10,
        width:'100%'
      },
      buttonText: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#fff', // Button text color
        textAlign: 'center',
      },
})
